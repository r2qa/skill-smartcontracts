# OS / editor cruft
.DS_Store
*.swp
*~
.idea/
.vscode/

# never commit secrets or per-user config (the FILLED-IN env), but DO track the template
audit.env
!audit.env.example
*.key
.env

# stray audit artifacts (this repo holds the skill, not review output)
findings/
out/
cache/
broadcast/
*.log
